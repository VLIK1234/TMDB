<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoginActivity">

    <ImageView
        android:id="@+id/background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/default_content_description"
        android:src="@drawable/cinema_background"
        android:scaleType="centerCrop"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical">

        <Button
            android:id="@+id/bt_vk_auth"
            android:layout_width="@dimen/login_button_width_size"
            android:layout_height="@dimen/login_button_height_size"
            android:background="@drawable/background_select_login_buttons"
            android:layout_margin="@dimen/login_button_margin"
            android:text="@string/button_log_vkauth" />

        <Button
            android:id="@+id/bt_no_auth"
            android:layout_width="@dimen/login_button_width_size"
            android:layout_height="@dimen/login_button_height_size"
            android:background="@drawable/background_select_login_buttons"
            android:layout_margin="@dimen/login_button_margin"
            android:text="@string/button_log_noauth" />

        <Button
            android:id="@+id/bt_alpha"
            android:layout_width="@dimen/login_button_width_size"
            android:layout_height="@dimen/login_button_height_size"
            android:layout_margin="@dimen/login_button_margin"
            android:text="@string/button_alpha"
            android:visibility="gone" />

    </LinearLayout>

</RelativeLayout>